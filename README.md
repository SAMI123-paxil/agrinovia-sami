# AI Model Module

This directory is reserved for Agrinovia AI research and implementation.

- `disease_detection/` — models and scripts for crop disease detection.
- `prediction/` — yield forecasting, irrigation advice, and crop growth models.
- `datasets/` — training datasets for imagery and sensor data.

Use TensorFlow and OpenCV in future development to build the AI crop monitoring system.
